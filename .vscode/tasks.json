{
    "tasks": [
        {
            "type": "shell",
            "label": "g++.exe build active file",
            "command": "g++",
            "args": [
                "-g",
                "${file}",
                "-std=c++11",
                "-o",
                // "${fileDirname}\\${fileBasenameNoExtension}.exe"
                "${fileBasenameNoExtension}.out"
            ]
            // ,
            // "options": {
            //     "cwd": "F:\\TDM-GCC-64\\bin"
            // }
        }
    ],
    "version": "2.0.0"
}